# Curated organism panels

This directory contains the bundled organism annotation panels used by Celltaminate:

- `kitome_and_background_blacklist.tsv`
- `clinically_important_pathogens.tsv`

These panels should be version-controlled because they are small, interpretable, and directly affect score annotation.
